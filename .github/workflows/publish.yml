name: CI & Publish (Verbose & Fixed)

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Show current directory & files
        run: |
          echo "=== Test Job: Current Directory ==="
          pwd
          echo "=== Listing files ==="
          ls -la

      - name: Show Node & npm versions
        run: |
          echo "=== Node & npm Versions ==="
          node -v
          npm -v
          npm config list

      - name: Install dependencies and run tests
        run: |
          echo "=== Installing dependencies ==="
          npm ci --verbose
          echo "=== Running tests ==="
          node tests.js

  publish:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code (with PAT)
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PERSONAL_GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Show current directory & files
        run: |
          echo "=== Publish Job: Current Directory ==="
          pwd
          echo "=== Listing files ==="
          ls -la

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: https://registry.npmjs.org/

      - name: Show Node & npm versions after setup
        run: |
          echo "=== Node & npm Versions ==="
          node -v
          npm -v
          npm config list

      - name: Configure Git
        run: |
          echo "=== Configuring Git ==="
          git config --list
          git config user.name "${{ github.actor }}"
          git config user.email "${{ github.actor }}@users.noreply.github.com"
          git config --list

      - name: Show git status
        run: |
          echo "=== Git Status Before Version Bump ==="
          git status
          git log -n 3 --oneline

      - name: Bump version and push
        run: |
          echo "=== Current package version ==="
          cat package.json | grep '"version"'
          echo "=== Bumping patch version ==="
          npm version patch --git-tag-version --verbose
          echo "=== Git status after version bump ==="
          git status
          git log -n 3 --oneline
          echo "=== Pushing to main ==="
          git push origin main --follow-tags --verbose

      - name: NPM debug info
        run: |
          echo "=== NPM Debug Info ==="
          npm whoami || echo "Token not valid"
          npm config list

      - name: Publish to npm
        run: |
          echo "=== Publishing to npm ==="
          npm publish --verbose
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: List dist folder
        run: |
          echo "=== Contents of dist/ ==="
          ls -la dist/
